{
    "name": "psphost/paymentpage-sdk",
    "description": "PSPHost SDK",
    "type": "library",
    "authors": [
        {
            "name": "PSPHost",
            "email": "sdk@psphost.com "
        }
    ],
    "autoload": {
        "psr-4": { "psphost\\": "src/" }
    },
    "require": {
    },
    "require-dev": {
        "phpunit/phpunit": "^6.5||^7.2",
        "squizlabs/php_codesniffer": "^3.3"
    },
    "scripts": {
        "lint": "phpcs --report=full --report-width=120",
        "test": "phpunit --coverage-clover clover.xml"
    }
}
